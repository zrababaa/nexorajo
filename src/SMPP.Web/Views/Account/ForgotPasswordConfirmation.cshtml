@{
    ViewData["Title"] = L["Check your email"];
}

<p>@L["If an account with that email exists, a password reset link has been sent."]</p>

<div class="text-center mt-3 small">
    <a asp-controller="Account" asp-action="Login">@L["Back to sign in"]</a>
</div>
